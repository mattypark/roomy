# Backend (backend/.env)
# Claude Vision deep analysis — leave unset to run in demo mode (local CV still works)
ANTHROPIC_API_KEY=

# Frontend (frontend/.env.local)
NEXT_PUBLIC_API_URL=http://localhost:8000
